#!/bin/bash
clear
echo "========================================"
echo "    Instalador SDIIN INTERNET ILIMITADO"
echo "         by Favio (botcito repo)"
echo "========================================"
echo
sleep 1
echo "🔧 Actualizando el sistema..."
apt update -y && apt upgrade -y

echo
echo "📥 Descargando script principal..."
wget --inet4-only https://raw.githubusercontent.com/favio/botcito/main/ssh-admin.sh -O /usr/local/bin/ssh-admin.sh

chmod +x /usr/local/bin/ssh-admin.sh

echo
echo "✅ Instalación completa!"
echo
echo "Usa el comando: ssh-admin.sh help"
echo
echo "Ejemplo rápido:"
echo "  ssh-admin.sh install     → instala SSH + Dropbear"
echo "  ssh-admin.sh create user pass 3 2 → crea usuario 3 días, máx 2 conexiones"
echo
echo "Gracias por usar SDIIN INTERNET ILIMITADO 🚀"
